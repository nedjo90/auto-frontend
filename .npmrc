; Azure Artifacts registry for @auto scope
; REQUIRED for CI - uncomment and configure when Azure Artifacts feed is ready
; For local development, @auto/shared uses file: protocol (see package.json)
; @auto:registry=https://pkgs.dev.azure.com/<ORG>/_packaging/<FEED>/npm/registry/
; //pkgs.dev.azure.com/<ORG>/_packaging/<FEED>/npm/registry/:_authToken=${AZURE_ARTIFACTS_TOKEN}
; //pkgs.dev.azure.com/<ORG>/_packaging/<FEED>/npm/:_authToken=${AZURE_ARTIFACTS_TOKEN}
